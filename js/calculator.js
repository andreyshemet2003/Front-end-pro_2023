const a = Number(prompt('what is the first number?'));
const b = Number(prompt('what is the second number?'));
let sum = a + b;
let difference = a - b;
let product = a * b;
let quotient = a / b;
alert (`${a} + ${b} = ${sum}\n${a} - ${b} = ${difference}\n${a} * ${b} = ${product}\n${a} / ${b} = ${quotient}`);
